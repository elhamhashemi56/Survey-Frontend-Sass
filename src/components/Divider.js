import React from 'react';
import './divider.scss'

const Divider = (props) => {
    return (
        <div id={props.id} className='divider-Container'>
           
        </div>
    );
};

export default Divider;